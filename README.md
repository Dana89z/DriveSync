# DriveSync

This is made for my pc, if you change a few variables, can make it yours.


Basically, dumps a folder into google drive, in a folder specifically called Python. The file is named "Python_(day_hour_minute_second)"
If drive is not opened, opens it and runs back the script
That's it 🐸
